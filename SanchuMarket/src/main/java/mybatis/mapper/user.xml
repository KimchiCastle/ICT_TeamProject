<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	
	<!-- 회원목록 조회  -->
	<select id="user_list" resultType="vo.UserVo">
		select * from user_market order by u_idx
	</select>
	
	<select id="user_idx" resultType="vo.UserVo">
		select * from user_market where u_idx=#{u_idx} 
	</select>
	
	<select id="check_id" resultType="vo.UserVo">
		select * from user_market where u_id=#{u_id} 
	</select>
	
	<select id="check_nickname" resultType="vo.UserVo">
		select * from user_market where u_nickname=#{u_nickname} 
	</select>
	<!--u_photo/u_like는 처음 가입시 null/0-->
	<insert id="user_insert" parameterType="vo.UserVo">
	insert into user_market values((select nvl(max(u_idx)+1,1) from user_market),#{u_id}, #{u_pwd}, #{u_name}, #{u_nickname},
		                               #{u_birth}, #{u_addr}, sysdate, 0, #{u_profile},
									   '', #{u_tel}, #{u_mail}, '일반회원', #{u_ip}
	</insert> 
		
	
	
</mapper>













<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
	<!-- 회원목록 조회  -->
	<select id="user_list" resultType="vo.UserVo">
		select * from user_market order by u_idx
	</select>
	
	<select id="user_idx" resultType="vo.UserVo">
		select * from user_market where u_idx=#{u_idx} 
	</select>
	
	<select id="check_id" resultType="vo.UserVo">
		select u_id, u_pwd from user_market where u_id=#{u_id} 
	</select>
	
	<select id="user_insert" parameterType="vo.UserVo">
		insert into user_market values(u_id=(select nvl(max(u_idx)+1,1) from user_market), u_pwd=#{u_pwd}, u_name=#{u_name}, u_nickname=#{u_nickname},
		                               u_birth=#{u_birth}, u_addr=#{u_addr}, u_regdate=sysdate, u_like=#{u_like}, u_profile=#{u_profile},
									   u_photo=#{u_photo}, u_tel=#{u_tel}, u_mail=#{u_mail},u_grade=#{u_grade) 
	</select>
	
	
</mapper>












